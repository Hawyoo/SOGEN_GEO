# SOGEN-GEO 检索工具 —— 依赖说明
#
# 运行时零第三方依赖：检索引擎(search_datasets.py)只用 Python 标准库，
# Python ≥ 3.8 即可，不需要 pip install 任何东西。
#
# 本文件存在的意义只是说明这一点，并登记唯一的"可选增强"：
#
#   py7zr —— 仅当您手里的交付压缩包是 .7z / .rar 格式、且电脑上没装
#            7-Zip 软件时才用得上(脚本会自动优先调用 7-Zip；都没有才
#            回退到 py7zr；.zip 用标准库直接解，永远不需要它)。
#            装了它也不影响其他任何功能。
#
# 想装：pip install -r requirements.txt
# 不装：一切照常，工具会在真正需要时给出当时的安装指引。

py7zr>=0.20; python_version >= "3.8"
